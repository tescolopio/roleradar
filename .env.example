# RoleRadar Environment Configuration Example
# Copy this file to .env and fill in your values
# IMPORTANT: With secure configuration, sensitive values are encrypted

# ===================================================
# API Keys - MIGRATE TO SECURE STORAGE
# ===================================================
# Run: python secure_config_manager.py init
# Then: python secure_config_manager.py migrate
# This will encrypt your API keys securely
TAVILY_API_KEY=
GROQ_API_KEY=

# ===================================================
# Database Configuration
# ===================================================
# These can remain in .env or migrate to secure storage
DB_HOST=localhost
DB_PORT=5433
DB_NAME=roleradar
DB_USER=roleradar
DB_PASSWORD=

# Alternatively, use a single DATABASE_URL
# DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# ===================================================
# Redis Configuration
# ===================================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# ===================================================
# Application Configuration
# ===================================================
FLASK_SECRET_KEY=
FLASK_ENV=development
FLASK_HOST=0.0.0.0
FLASK_PORT=5000

# ===================================================
# RoleRadar Scheduling & Search Configuration
# ===================================================

# Timezone for scheduled searches
# See https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TIMEZONE=America/New_York

# Scheduled search times (JSON format, 24-hour)
# Examples:
#   ["08:00", "12:00", "15:00"]  - 3 times daily
#   ["09:00"]                     - Once at 9 AM
#   ["06:00", "10:00", "14:00", "18:00"]  - Every 4 hours
SCHEDULE_TIMES=["08:00", "12:00", "15:00"]

# Search roles (JSON format)
# These define what job titles to search for
# Leave empty to use defaults, or customize:
SEARCH_ROLES=["security engineer", "compliance officer", "GRC analyst", "CISO"]

# ===================================================
# SECURITY BEST PRACTICES
# ===================================================
# 
# For production use, we STRONGLY RECOMMEND using the secure
# configuration manager instead of storing credentials in .env:
#
# 1. Initialize secure storage:
#    python secure_config_manager.py init
#
# 2. Migrate existing credentials:
#    python secure_config_manager.py migrate
#
# 3. Remove sensitive values from .env
#
# Benefits of secure storage:
#   ✓ AES-256 encryption for all credentials
#   ✓ Master password protection
#   ✓ No plaintext API keys in files
#   ✓ Secure file permissions (0600)
#   ✓ PBKDF2 key derivation (390k iterations)
#
# View configuration:
#   python secure_config_manager.py show
#
# Update values securely:
#   python secure_config_manager.py set-key TAVILY_API_KEY
#   python secure_config_manager.py set-roles "role1, role2, role3"
#
